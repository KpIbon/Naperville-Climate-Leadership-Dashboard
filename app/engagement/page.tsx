'use client';

import { ArrowUpRight, BrainCircuit, Gauge, Lightbulb, Target, Users } from 'lucide-react';
import { Card, SectionTitle, Shell } from '@/components/shell';
import { organizations } from '@/lib/data';

export default function Engagement() { const priority = [...organizations].sort((a, b) => b.engagement - a.engagement).slice(0, 8); return <Shell eyebrow="Commercial engagement score" title="Focus outreach where it can compound." subtitle="An operations-research-inspired prioritization model turns a broad prospect list into a practical next conversation."><div className="mb-4 flex items-center gap-2 rounded-2xl border border-[#b9d6ca] bg-mint/60 p-4 text-xs text-moss dark:bg-[#1c352a] dark:text-lime"><BrainCircuit size={18} /><span><strong>Model lens:</strong> employee count, visibility, customer traffic, existing sustainability efforts, and potential influence are weighted into a simulated 0–100 priority score.</span></div><div className="grid gap-4 lg:grid-cols-[1.3fr_.7fr]"><Card><SectionTitle title="Priority queue" detail="Highest modeled outreach opportunity" /><div className="space-y-3">{priority.map((org, index) => <div key={org.name} className="grid grid-cols-[30px_1fr_auto] items-center gap-3 rounded-xl border border-[var(--border)] p-3"><span className="text-center font-display text-sm font-semibold text-[var(--muted)]">{String(index + 1).padStart(2, '0')}</span><div><p className="text-xs font-semibold">{org.name}</p><div className="mt-1 flex flex-wrap gap-1.5"><span className="rounded-full bg-[#edf1ff] px-2 py-0.5 text-[9px] text-[#5567a2]">{org.industry}</span><span className="rounded-full bg-[#fff7dc] px-2 py-0.5 text-[9px] text-[#9e7822]">{org.recommendation}</span></div></div><div className="text-right"><p className="font-display text-xl font-semibold text-moss">{org.engagement}</p><p className="text-[9px] uppercase tracking-wider text-[var(--muted)]">priority</p></div></div>)}</div></Card><div className="space-y-4"><Card><SectionTitle title="Score composition" detail="Weighted model inputs" />{[['Potential influence', 30], ['Community visibility', 25], ['Customer traffic', 20], ['Sustainability signals', 15], ['Employee count', 10]].map(([label, value]) => <div key={String(label)} className="mb-4 last:mb-0"><div className="mb-1 flex justify-between text-[11px]"><span>{label}</span><span className="font-bold">{value}%</span></div><div className="h-2 rounded-full bg-[#e8eee9]"><div className="h-2 rounded-full bg-coral" style={{ width: `${value}%` }} /></div></div>)}</Card><Card className="bg-[#fffaf0] dark:bg-[#332d20]"><Lightbulb className="text-[#b8852b]" size={20} /><h3 className="mt-4 font-display text-lg font-semibold">Recommended next move</h3><p className="mt-2 text-xs leading-5 text-[var(--muted)]">Start with high-visibility organizations that already tell a sustainability story. They can become trusted peer messengers for smaller businesses.</p><a href="/directory" className="mt-4 flex items-center gap-1 text-xs font-bold text-moss dark:text-lime">Review the directory <ArrowUpRight size={14} /></a></Card></div></div></Shell>; }
