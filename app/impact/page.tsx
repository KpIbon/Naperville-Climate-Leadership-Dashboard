'use client';

import { ArrowDownRight, Droplets, Home, Lightbulb, Sun, Wind } from 'lucide-react';
import { Card, SectionTitle, Shell } from '@/components/shell';
import { ImpactChart } from '@/components/charts';

export default function Impact() { const allocations = [{ name: 'Solar projects', value: 39, amount: '$24.8k', icon: Sun, color: '#f3c95b' }, { name: 'Building insulation', value: 27, amount: '$17.1k', icon: Home, color: '#7a9f42' }, { name: 'Window replacement', value: 21, amount: '$13.3k', icon: Wind, color: '#7992c5' }, { name: 'Community education', value: 13, amount: '$8.3k', icon: Lightbulb, color: '#e88968' }]; return <Shell eyebrow="Community impact dashboard" title="Turn contributions into visible outcomes." subtitle="A modeled allocation view helps City leaders explain where voluntary investment can show up in the community."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['78 t', 'CO₂e reduced', Droplets], ['124 MWh', 'energy savings', Lightbulb], ['12', 'projects modeled', Sun], ['$63.5k', 'simulated funding', ArrowDownRight]].map(([value, label, Icon]) => <Card key={String(label)}><Icon size={18} className="text-moss" /><p className="mt-5 font-display text-2xl font-semibold">{value as string}</p><p className="mt-1 text-xs text-[var(--muted)]">{label as string}</p></Card>)}</div><div className="mt-4 grid gap-4 lg:grid-cols-[.9fr_1.1fr]"><Card><SectionTitle title="Funding allocation" detail="$63.5k simulated investment envelope" /><div className="space-y-5">{allocations.map((item) => <div key={item.name} className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl" style={{ background: `${item.color}25`, color: item.color }}><item.icon size={18} /></div><div className="flex-1"><div className="flex justify-between text-xs"><span className="font-semibold">{item.name}</span><span>{item.amount}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-[#edf0ed]"><div className="h-full rounded-full" style={{ width: `${item.value * 2.2}%`, background: item.color }} /></div></div><span className="w-8 text-right text-[11px] font-bold text-[var(--muted)]">{item.value}%</span></div>)}</div><div className="mt-7 rounded-xl bg-mint p-4 text-xs leading-5 text-moss dark:bg-[#1d3a2e] dark:text-lime">Modeled outcomes use transparent assumptions for demonstration. Final project selection would be co-designed with participating organizations and community partners.</div></Card><Card><SectionTitle title="Impact trajectory" detail="Modeled cumulative outcomes over the program year" /><div className="mb-4 flex gap-4 text-[11px] text-[var(--muted)]"><span><i className="mr-1 inline-block size-2 rounded-full bg-coral" />CO₂e reduced</span><span><i className="mr-1 inline-block size-2 rounded-full bg-[#7a9f42]" />Energy saved</span></div><div className="h-[300px]"><ImpactChart /></div></Card></div></Shell>; }
