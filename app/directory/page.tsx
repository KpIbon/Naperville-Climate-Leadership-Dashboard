'use client';

import { useMemo, useState } from 'react';
import { ArrowUpRight, Search, SlidersHorizontal, Users } from 'lucide-react';
import { Card, SectionTitle, Shell } from '@/components/shell';
import { organizations } from '@/lib/data';

export default function Directory() { const [query, setQuery] = useState(''); const [filter, setFilter] = useState('All sectors'); const filtered = useMemo(() => organizations.filter((org) => `${org.name} ${org.industry} ${org.address}`.toLowerCase().includes(query.toLowerCase()) && (filter === 'All sectors' || org.industry === filter)), [query, filter]); const sectors = ['All sectors', ...Array.from(new Set(organizations.map((org) => org.industry)))]; return <Shell eyebrow="Green business directory" title="Meet Naperville's climate community." subtitle="Explore a modeled directory of local organizations and the climate pathways they could make visible."><Card><div className="flex flex-col gap-3 md:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--muted)]" size={16} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search organizations, sectors, or neighborhoods" className="h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] pl-10 pr-4 text-sm outline-none ring-moss/30 focus:ring-2" /></label><label className="relative"><SlidersHorizontal className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[var(--muted)]" size={15} /><select value={filter} onChange={(e) => setFilter(e.target.value)} className="h-11 w-full appearance-none rounded-xl border border-[var(--border)] bg-[var(--background)] pl-9 pr-8 text-xs font-semibold outline-none md:w-56">{sectors.map((sector) => <option key={sector}>{sector}</option>)}</select></label></div><div className="mt-6 flex items-center justify-between"><SectionTitle title={`${filtered.length} organizations`} detail="Publicly identifiable local organizations with simulated demonstration metrics" /><span className="text-[11px] text-[var(--muted)]">Sorted by contribution</span></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{filtered.map((org) => <article key={org.name} className="group rounded-2xl border border-[var(--border)] bg-[var(--background)] p-4 transition hover:-translate-y-1 hover:border-moss/40 hover:shadow-panel"><div className="flex items-start justify-between"><div className="flex size-10 items-center justify-center rounded-xl bg-mint text-sm font-bold text-moss">{org.name.split(' ').map((w) => w[0]).slice(0, 2).join('')}</div><span className="rounded-full bg-mint px-2 py-1 text-[10px] font-bold text-moss">{org.certification}</span></div><h3 className="mt-4 text-sm font-semibold">{org.name}</h3><p className="mt-1 text-[11px] text-[var(--muted)]">{org.industry} · {org.address}</p><div className="mt-4 grid grid-cols-2 gap-3 border-t border-[var(--border)] pt-3"><div><p className="text-[10px] text-[var(--muted)]">Modeled contribution</p><p className="mt-1 text-sm font-semibold">${org.contribution.toLocaleString()}</p></div><div><p className="text-[10px] text-[var(--muted)]">Community reach</p><p className="mt-1 flex items-center gap-1 text-sm font-semibold"><Users size={13} className="text-moss" />{org.reach.toLocaleString()}</p></div></div><a href="/engagement" className="mt-4 flex items-center gap-1 text-[11px] font-bold text-moss dark:text-lime">View opportunity <ArrowUpRight size={13} /></a></article>)}</div></Card></Shell>; }
